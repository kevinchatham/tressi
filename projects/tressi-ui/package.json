{
  "name": "tressi-ui",
  "private": true,
  "scripts": {
    "clean": "npx rimraf --glob \"**/.angular\"",
    "serve": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}
