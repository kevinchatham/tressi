{
  "name": "tressi-ui",
  "private": true,
  "scripts": {
    "clean": "npx rimraf --glob \"**/.angular\"",
    "serve": "npm run clean && ng serve",
    "build": "npm run clean && ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}
