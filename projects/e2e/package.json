{
  "name": "@tressi/e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:single": "playwright test tests/rps.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  }
}
