{
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  },
  "name": "@tressi/e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:single": "playwright test tests/cli.spec.ts",
    "test:ui": "playwright test --ui"
  }
}
